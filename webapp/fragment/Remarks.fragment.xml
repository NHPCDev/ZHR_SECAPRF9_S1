<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
>
    <Dialog
        id="idDecisionDialog"
        title="{i18n>DialogTitleRemarks}"
        type="Message" 
        titleAlignment="Center" 
		class="sapUiSizeCompact"
    >
        <content>
            <VBox id="idVbDicision">               
                <Text text="{= ${viewModel>/decision} === 'Approve' ? ${i18n>approveText} : ${i18n>returnText}}" class="sapUiSmallMarginBottom"></Text>
                <Label id="idVbDicisionLabel1"
                    text="{i18n>RemarksLabel}"
                    required="{= ${viewModel>/decision} !== 'Approve'}"
                />
                <TextArea
                    id="idRemarks"
                    rows="2"
                    width="100%"
                    maxLength="230"
                    value="{viewModel>/formDetails/Remarks}"
                    valueState="{viewModel>/valueState/Remarks}"
                    valueStateText="{viewModel>/valueStateText/Remarks}"
                    liveChange="onRemarksChange"
                />
            </VBox>
        </content>
        <beginButton>
            <Button
                id="idBtnSbmtApp"
                text="{i18n>subBtnText}"
                type="Emphasized"
                press="onDecisionSubmit"
                enabled="{= ${viewModel>/submitEnabled} === 'true'}"
            />
        </beginButton>
        <endButton>
            <Button
                id="idBtnCancelApp"
                text="{i18n>cancelBtnText}"
                press="onDecisionCancel"
            />
        </endButton>
    </Dialog>
</core:FragmentDefinition>